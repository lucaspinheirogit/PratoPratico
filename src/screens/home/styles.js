import {
  StyleSheet
} from 'react-native';

export default StyleSheet.create({
  logo: {
    flex: 3,
    justifyContent: 'center',
    marginBottom: 20,
  },
  loginView: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 20,
  },
});
